import { Shape } from "orbz";

export const PaletteGenerator = Shape({
  [Shape.name]: "ArgiopePaletteGenerator",
  saturation: 0.66,
  lMin: 0.32,
  lMax: 0.88,
  neutralSaturation: 0.08,
  neutralLMin: 0.45,
  neutralLMax: 0.86,
  fullSaturation: false,
});
